import { useEffect, useState } from "react";
import { Link, useNavigate } from "react-router-dom";
import { ArrowLeft, Plus, Radio, Save, Loader2 } from "lucide-react";
import { useAuth } from "../context/AuthContext";
import { channelAPI } from "../services/channel/channelApi";

const premium=(user)=>Boolean(user&&user.status&&String(user.status).toLowerCase()!=="free");
export default function ChannelManagePage(){const {user}=useAuth();const nav=useNavigate();const [channels,setChannels]=useState([]);const [form,setForm]=useState({name:"",description:"",category:"",type:"PUBLIC"});const [loading,setLoading]=useState(true);const [saving,setSaving]=useState(false);const [error,setError]=useState("");useEffect(()=>{channelAPI.list().then(r=>setChannels((r.channels||[]).filter(c=>String(c.ownerId)===String(user?.id)))).catch(e=>setError(e?.response?.data?.message||"Unable to load channels.")).finally(()=>setLoading(false))},[user?.id]);if(!premium(user))return <section className="min-h-screen w-full bg-neutral-950 p-6 text-neutral-300"><Link to="/channels" className="text-xs text-neutral-500">← Channels</Link><div className="mx-auto mt-20 max-w-md text-center"><Radio className="mx-auto text-neutral-700" size={40}/><h1 className="mt-4 text-xl font-black">Premium channel access</h1><p className="mt-2 text-sm text-neutral-500">Channel creation and studio management are available to premium accounts.</p></div></section>;const create=async(e)=>{e.preventDefault();if(!form.name.trim())return;setSaving(true);setError("");try{const c=await channelAPI.create(form);setChannels(x=>[c,...x]);setForm({name:"",description:"",category:"",type:"PUBLIC"})}catch(e){setError(e?.response?.data?.message||"Could not create channel.")}finally{setSaving(false)}};return <section className="min-h-screen w-full bg-neutral-950 px-4 pb-28 pt-5 text-neutral-100"><div className="mx-auto max-w-4xl"><Link to="/channels" className="inline-flex items-center gap-2 text-xs font-bold text-neutral-500 hover:text-white"><ArrowLeft size={16}/> Channels</Link><header className="mt-5"><h1 className="text-2xl font-black">Manage channels</h1><p className="mt-1 text-xs text-neutral-500">Create and manage your channel, programs, schedule, content, team and audience.</p></header><form onSubmit={create} className="mt-6 rounded-3xl border border-neutral-800 bg-neutral-900/60 p-5"><div className="grid gap-3 sm:grid-cols-2"><input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="Channel name" className="rounded-xl border border-neutral-800 bg-neutral-950 px-4 py-3 text-sm outline-none"/><input value={form.category} onChange={e=>setForm({...form,category:e.target.value})} placeholder="Category e.g. Music" className="rounded-xl border border-neutral-800 bg-neutral-950 px-4 py-3 text-sm outline-none"/><textarea value={form.description} onChange={e=>setForm({...form,description:e.target.value})} placeholder="Describe your channel" className="min-h-24 rounded-xl border border-neutral-800 bg-neutral-950 px-4 py-3 text-sm outline-none sm:col-span-2"/><select value={form.type} onChange={e=>setForm({...form,type:e.target.value})} className="rounded-xl border border-neutral-800 bg-neutral-950 px-4 py-3 text-sm"><option value="PUBLIC">Public</option><option value="PRIVATE">Private</option></select><button disabled={saving} className="inline-flex items-center justify-center gap-2 rounded-xl bg-neutral-100 px-4 py-3 text-xs font-black text-neutral-950 disabled:opacity-50">{saving?<Loader2 size={15} className="animate-spin"/>:<Plus size={15}/>}Create channel</button></div>{error&&<p className="mt-3 text-xs text-red-300">{error}</p>}</form><div className="mt-8"><h2 className="font-black">Your channels</h2>{loading?<Loader2 className="mt-6 animate-spin text-neutral-700"/>:channels.length?<div className="mt-3 grid gap-3 sm:grid-cols-2">{channels.map(c=><div key={c.id} className="rounded-2xl border border-neutral-800 bg-neutral-900/60 p-4"><div className="flex items-center justify-between"><div><h3 className="font-bold">{c.name}</h3><p className="mt-1 text-[10px] text-neutral-600">{c.category||"General"} · {c.type}</p></div><Link to={`/channels/${c.id}`} className="rounded-lg border border-neutral-700 px-3 py-2 text-[10px] font-bold">Open</Link></div><div className="mt-4 flex gap-2"><Link to={`/channels/${c.id}`} className="rounded-lg bg-neutral-100 px-3 py-2 text-[10px] font-black text-neutral-950">Studio</Link><span className="rounded-lg border border-neutral-800 px-3 py-2 text-[10px] text-neutral-500">Programs</span><span className="rounded-lg border border-neutral-800 px-3 py-2 text-[10px] text-neutral-500">Audience</span></div></div>)}</div>:<p className="mt-4 text-sm text-neutral-600">No channels yet. Create your first one above.</p>}</div></div></section>}
